name: CI build and run
on: [push]
jobs:
  configure:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v2

      - name: install-depends
        run: sudo apt-get install doxygen

      - name: configure
        run: ./configure;

      - name: build
        run: ./configure; ./build.sh

      - name: run
        run: ./configure; ./build.sh; ./run.sh
